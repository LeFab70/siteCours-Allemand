services:
  - type: web
    name: mag-lingua
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/mag-lingua/browser
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
