services:
  - type: web
    name: mag-lingua-institut
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/mag-lingua-institut/browser
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
